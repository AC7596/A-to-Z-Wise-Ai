name = "a-to-z-wise-ai-diagnosis-backend"
main = "worker.mjs"
compatibility_date = "2026-09-16"

[vars]
# Comma-separated list of allowed site origins for CORS, for example:
# ALLOWED_ORIGINS = "https://atozwiseai.com,https://www.atozwiseai.com"
ALLOWED_ORIGINS = "https://atozwiseai.com"
AI_PROVIDER_MODEL = "gpt-4o-mini"
AI_PROVIDER_BASE_URL = "https://api.openai.com/v1"

# Configure secrets in your platform (do NOT commit real values):
# wrangler secret put AI_PROVIDER_API_KEY
